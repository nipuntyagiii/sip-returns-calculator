# SIP Returns Calculator

A Python-based SIP (Systematic Investment Plan) returns calculator built
using modular architecture and unit tests.

## Formula
FV = P × [((1 + r)^n − 1) / r] × (1 + r)

## Features
- SIP future value calculation
- Input validation
- CLI interface
- Unit tests

## Run
python main.py

## Test
pytest
